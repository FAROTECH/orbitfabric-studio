{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal default capability for the initial OrbitFabric Studio shell. No filesystem, dialog or shell execution permissions are granted in this scaffold PR.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
