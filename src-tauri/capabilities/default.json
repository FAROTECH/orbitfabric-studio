{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for v0.1.0 workspace opening. It allows only the directory selection dialog plus the core baseline permission. Filesystem traversal is performed by the dedicated Rust inspection command.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
