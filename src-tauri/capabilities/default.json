{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Window-scoped capability for the source preview. It exposes only the Core baseline permission and the native directory-open dialog; mission resolution and Core process execution remain in dedicated Rust commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
