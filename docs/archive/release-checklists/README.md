# Historical Release Checklists

This directory preserves the historical root-level `V0_*_RELEASE_CHECKLIST.md` files.

They are archived because they document earlier release checkpoints and implementation phases, but they no longer represent the current Studio public-preview baseline.

The current project baseline is defined by the active README, roadmap, architecture documentation and the latest QA decision records.
